# bing-bg-scraper
Bing Lock screen Background image scraper

First run:
``` npm i ```

Usage:
``` run.bat ```

After run.bat, bing images and windows store images are copied to pics folder in the current directory where main.js is present.
